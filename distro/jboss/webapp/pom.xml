<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <groupId>org.flowave.bpm.jboss</groupId>
    <artifactId>flowave-jboss</artifactId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>

  <modelVersion>4.0.0</modelVersion>
  <groupId>org.flowave.bpm.webapp</groupId>
  <name>Flowave Platform - webapp - JBoss</name>
  <artifactId>flowave-webapp-jboss</artifactId>
  <packaging>war</packaging>
  
  <properties>
    <!-- generate a bom of compile time dependencies for the license book.
    Note: Every compile time dependency will end up in the license book. Please
    declare only dependencies that are actually needed -->
    <!-- if set to false, maven-deploy-plugin-2.8.2 fails with "status code: 400, reason phrase: Bad Request" -->
    <skip-third-party-bom>true</skip-third-party-bom>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.flowave.bpm.webapp</groupId>
      <artifactId>flowave-webapp</artifactId>
      <version>${project.version}</version>
      <type>war</type>
      <scope>runtime</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>camunda-webapp</finalName>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <configuration>
          <archive>
            <manifestEntries>
              <!-- module dependencies for deployment on JBoss/WildFly -->
              <Dependencies>org.flowave.bpm.flowave-engine,org.flowave.bpm.dmn.flowave-engine-dmn,org.flowave.bpm.model.flowave-bpmn-model,org.flowave.bpm.model.flowave-cmmn-model,org.flowave.bpm.model.flowave-dmn-model,org.flowave.bpm.model.flowave-xml-model,org.flowave.commons.flowave-commons-logging,org.flowave.spin.flowave-spin-core,org.flowave.bpm.juel.flowave-juel services,org.graalvm.js.js-scriptengine services</Dependencies>
            </manifestEntries>
          </archive>          
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
