package org.finos.fluxnova;

/**
 * Test marker interface used to verify that proxy classes are created
 * for remapped legacy interface names (org.camunda -> org.finos.fluxnova).
 */
public interface TestProxyInterface {
    // intentionally empty for proxy tests
}

